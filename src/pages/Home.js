import UserResults from '../components/users/UserResults';

const Home = () => {
  return (
    <div>
      {/* Search component comes here */}
      <UserResults />
    </div>
  );
};

export default Home;
